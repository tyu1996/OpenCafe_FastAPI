# This file makes the schemas directory a Python package
# It allows imports like: from interfaces.api.schemas.orders import OrderResponse

# We can optionally re-export common schemas here for convenience:
# from .menu import MenuItemResponse
# from .orders import OrderResponse, CreateOrderRequest
# from .tables import TableResponse

# But for now, we keep it empty and let users import directly from submodules
